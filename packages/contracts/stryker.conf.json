{
  "$schema": "../../node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "packageManager": "pnpm",
  "testRunner": "vitest",
  "plugins": ["@stryker-mutator/vitest-runner"],
  "vitest": { "configFile": "vitest.config.ts" },

  "//": "Scoped to the files that hold logic rather than declarations. Mutating `z.string().min(1)` on a request schema mostly produces mutants nothing can distinguish — the schema IS the assertion — and the API's own validation and e2e suites are what exercise those. buildReadiness and the status mapping are different: a mistake there escapes to every client, so they are what is worth mutating.",
  "mutate": ["src/constants.ts", "src/health.ts", "!src/**/*.spec.ts"],

  "reporters": ["clear-text", "progress", "html", "json"],
  "htmlReporter": { "fileName": "reports/mutation/index.html" },
  "jsonReporter": { "fileName": "reports/mutation/report.json" },

  "//thresholds": "break is null on purpose: report, do not gate. See the note in packages/domain/stryker.conf.json.",
  "thresholds": { "high": 80, "low": 60, "break": null },

  "tempDirName": ".stryker-tmp"
}
